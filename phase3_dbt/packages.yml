packages:
  # Standard dbt helper macros: surrogate keys, cross-db functions, generic
  # tests (e.g. accepted_range) used across the mart layer.
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]

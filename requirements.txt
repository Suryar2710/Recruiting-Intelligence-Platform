# Consolidated project requirements (used by CI).
# Pinned loosely to the versions verified locally on this project.

# Phase 1: synthetic data generation
numpy>=1.26,<2.0
pandas>=2.0,<3.0
faker>=25.0

# Phase 2: Postgres load
psycopg[binary]>=3.1

# Phase 3: dbt
dbt-core>=1.8,<2.0
dbt-postgres>=1.8,<2.0

# Phase 4: data quality
great_expectations>=0.18,<0.19
sqlalchemy>=1.4,<2.1
psycopg2-binary>=2.9
